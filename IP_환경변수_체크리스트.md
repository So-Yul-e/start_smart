# IP 환경변수 처리 체크리스트

## ✅ 이미 처리된 부분

### 1. 프론트엔드 API 설정 (`frontend/config.js`)
- ✅ 현재 호스트 기반으로 자동 설정
- ✅ `http://172.16.48.47:3000/input/` → `http://172.16.48.47:3000` 자동 감지
- ✅ `window.API_BASE_URL` 우선 사용 가능

### 2. 백엔드 서버 (`backend/server.js`)
- ✅ `HOST` 환경변수 사용 (기본값: `localhost`)
- ✅ `PORT` 환경변수 사용 (기본값: `3000`)

### 3. 프론트엔드 입력 페이지 (`frontend/input/script.js`)
- ✅ `window.API_CONFIG.API_BASE_URL` 사용
- ✅ 동적 API URL 설정

## ⚠️ 수동 설정이 필요한 부분

### 1. 백엔드 서버 실행 시

**방법 1: 환경변수로 설정**
```bash
HOST=172.16.48.47 PORT=3000 npm start
```

**방법 2: .env 파일에 추가**
```bash
# .env 파일
HOST=172.16.48.47
PORT=3000
```

### 2. 테스트 스크립트 실행 시

**전체 플로우 테스트:**
```bash
API_BASE_URL=http://172.16.48.47:3000 npm run test:flow
```

**또는 .env 파일에 추가:**
```bash
# .env 파일
API_BASE_URL=http://172.16.48.47:3000
```

## 📋 체크리스트

- [ ] `.env` 파일에 `HOST=172.16.48.47` 추가 (서버 실행용)
- [ ] `.env` 파일에 `API_BASE_URL=http://172.16.48.47:3000` 추가 (테스트용)
- [ ] 서버 실행 시 `HOST` 환경변수 확인
- [ ] 테스트 실행 시 `API_BASE_URL` 환경변수 확인

## 🔍 확인 방법

### 서버 실행 확인
```bash
# 서버 시작
HOST=172.16.48.47 npm start

# 출력 확인
# 🚀 Server running on http://172.16.48.47:3000
# 🌐 다른 기기에서 접근: http://172.16.48.47:3000
```

### 프론트엔드 API URL 확인
브라우저 콘솔에서:
```javascript
console.log(window.API_CONFIG.API_BASE_URL);
// 예상 출력: http://172.16.48.47:3000
```

### 테스트 스크립트 확인
```bash
API_BASE_URL=http://172.16.48.47:3000 npm run test:flow
```

## 🚨 주의사항

1. **서버와 테스트 스크립트의 IP가 일치해야 함**
   - 서버: `HOST=172.16.48.47`
   - 테스트: `API_BASE_URL=http://172.16.48.47:3000`

2. **프론트엔드는 자동 감지**
   - 별도 설정 불필요
   - 현재 접속한 URL 기반으로 자동 설정

3. **환경변수 우선순위**
   - 서버: `HOST` 환경변수 > 기본값 `localhost`
   - 테스트: `API_BASE_URL` 환경변수 > 기본값 `http://localhost:3000`
   - 프론트: `window.API_BASE_URL` > 현재 호스트 > `localhost:3000`
